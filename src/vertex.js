export class Vertex {
  constructor(x, y, z) {
    this.x = x;
    this.y = y;
    this.z = z;
  }
}

export class Vertex2D {
  constructor(x, y) {
    this.x = parseFloat(x);
    this.y = parseFloat(y);
  }
}